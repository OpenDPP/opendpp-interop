{
  "name": "@opendpp/interop-validate",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Validate AAS / UNTP DPP output against the official schemas OpenDPP's CI uses — offline, no product source required.",
  "license": "Apache-2.0",
  "homepage": "https://github.com/OpenDPP/opendpp-interop",
  "bin": {
    "opendpp-interop-validate": "./validate.mjs"
  },
  "scripts": {
    "validate": "node validate.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1"
  }
}
